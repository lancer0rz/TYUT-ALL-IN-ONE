# 实验五：简易论坛开发

## 一、实验目的

1. 综合运用前端技术（HTML5、CSS3、JavaScript）和后端技术（Servlet、JSP、JDBC）开发完整的Web应用
2. 掌握MVC设计模式在Web开发中的应用
3. 理解和实现用户认证与会话管理机制
4. 掌握数据库设计与JDBC编程技术
5. 培养独立开发完整Web应用的能力

## 二、实验环境

- **操作系统**：Windows
- **开发工具**：VS Code / IntelliJ IDEA
- **JDK版本**：JDK 11
- **服务器**：Apache Tomcat 9.x
- **数据库**：MySQL 8.0
- **构建工具**：Maven
- **浏览器**：Chrome / Firefox

## 三、实验内容

### 3.1 项目概述

开发一个简易论坛系统，实现以下功能：
- 用户注册、登录、退出
- 发表帖子
- 浏览帖子列表和详情
- 发表评论
- 分页显示

### 3.2 数据库设计

#### 3.2.1 数据库表结构

```sql
-- 用户表
CREATE TABLE user (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    nickname VARCHAR(50) NOT NULL,
    email VARCHAR(100),
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 文章表
CREATE TABLE article (
    id INT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(200) NOT NULL,
    content TEXT NOT NULL,
    user_id INT NOT NULL,
    author VARCHAR(50) NOT NULL,
    view_count INT DEFAULT 0,
    comment_count INT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES user(id)
);

-- 评论表
CREATE TABLE comment (
    id INT PRIMARY KEY AUTO_INCREMENT,
    article_id INT NOT NULL,
    user_id INT NOT NULL,
    author VARCHAR(50) NOT NULL,
    content TEXT NOT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (article_id) REFERENCES article(id),
    FOREIGN KEY (user_id) REFERENCES user(id)
);
```

### 3.3 项目结构

```
实验五/
├── pom.xml
├── sql/
│   └── init.sql
└── src/main/
    ├── java/com/forum/
    │   ├── entity/
    │   │   ├── User.java
    │   │   ├── Article.java
    │   │   └── Comment.java
    │   ├── dao/
    │   │   ├── UserDao.java
    │   │   ├── ArticleDao.java
    │   │   └── CommentDao.java
    │   ├── servlet/
    │   │   ├── RegisterServlet.java
    │   │   ├── LoginServlet.java
    │   │   ├── LogoutServlet.java
    │   │   ├── IndexServlet.java
    │   │   ├── ArticleServlet.java
    │   │   └── CommentServlet.java
    │   ├── filter/
    │   │   └── CharacterEncodingFilter.java
    │   └── util/
    │       └── DBUtil.java
    └── webapp/
        ├── css/
        │   └── style.css
        └── WEB-INF/
            ├── web.xml
            └── views/
                ├── includes/
                │   ├── header.jsp
                │   └── footer.jsp
                ├── error/
                │   ├── 404.jsp
                │   └── 500.jsp
                ├── register.jsp
                ├── login.jsp
                ├── index.jsp
                ├── article.jsp
                └── post.jsp
```

### 3.4 核心功能实现

#### 3.4.1 MVC架构设计

采用经典的MVC（Model-View-Controller）设计模式：

- **Model层**：Entity实体类（User、Article、Comment）+ DAO数据访问类
- **View层**：JSP页面，使用JSTL标签库
- **Controller层**：Servlet处理请求和业务逻辑

#### 3.4.2 数据库工具类

```java
public class DBUtil {
    private static final String URL = "jdbc:mysql://localhost:3306/forum_db";
    private static final String USER = "root";
    private static final String PASSWORD = "123456";
    
    public static Connection getConnection() throws SQLException {
        return DriverManager.getConnection(URL, USER, PASSWORD);
    }
}
```

#### 3.4.3 DAO模式实现

以ArticleDao为例：

```java
public class ArticleDao {
    public List<Article> findAll(int page, int pageSize) {
        List<Article> articles = new ArrayList<>();
        String sql = "SELECT * FROM article ORDER BY create_time DESC LIMIT ?, ?";
        try (Connection conn = DBUtil.getConnection();
             PreparedStatement pstmt = conn.prepareStatement(sql)) {
            pstmt.setInt(1, (page - 1) * pageSize);
            pstmt.setInt(2, pageSize);
            ResultSet rs = pstmt.executeQuery();
            while (rs.next()) {
                articles.add(mapResultSetToArticle(rs));
            }
        } catch (SQLException e) {
            e.printStackTrace();
        }
        return articles;
    }
}
```

#### 3.4.4 Servlet控制器

使用@WebServlet注解配置URL映射：

```java
@WebServlet("/article/*")
public class ArticleServlet extends HttpServlet {
    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response) {
        String pathInfo = request.getPathInfo();
        if ("/view/".equals(pathInfo)) {
            view(request, response);
        } else if ("/post".equals(pathInfo)) {
            post(request, response);
        }
    }
}
```

#### 3.4.5 JSP视图层

使用JSTL和EL表达式展示数据：

```jsp
<c:forEach var="article" items="${articles}">
    <div class="article-item">
        <h3><a href="${pageContext.request.contextPath}/article/view/${article.id}">
            ${article.title}
        </a></h3>
        <p>${article.summary}</p>
        <span>作者：${article.author}</span>
        <span>浏览：${article.viewCount}</span>
    </div>
</c:forEach>
```

#### 3.4.6 会话管理

使用HttpSession管理用户登录状态：

```java
// 登录成功后创建Session
HttpSession session = request.getSession();
session.setAttribute("user", user);
session.setAttribute("userId", user.getId());
session.setMaxInactiveInterval(30 * 60);

// 退出时销毁Session
HttpSession session = request.getSession(false);
if (session != null) {
    session.invalidate();
}
```

#### 3.4.7 字符编码过滤器

使用Filter确保中文字符正确处理：

```java
public class CharacterEncodingFilter implements Filter {
    @Override
    public void doFilter(ServletRequest request, ServletResponse response, 
                         FilterChain chain) {
        request.setCharacterEncoding("UTF-8");
        response.setCharacterEncoding("UTF-8");
        chain.doFilter(request, response);
    }
}
```

### 3.5 功能展示

#### 3.5.1 用户注册

- 表单验证：用户名、密码、昵称必填
- 密码确认验证
- 用户名唯一性检查
- 注册成功后跳转登录页

#### 3.5.2 用户登录

- 验证用户名和密码
- 支持"记住用户名"功能（Cookie）
- 登录成功创建Session

#### 3.5.3 帖子列表

- 分页显示帖子
- 显示标题、摘要、作者、时间
- 显示浏览量和评论数

#### 3.5.4 发表帖子

- 登录用户可发帖
- 标题和内容验证
- 自动记录作者信息

#### 3.5.5 帖子详情与评论

- 显示完整帖子内容
- 增加浏览计数
- 评论列表展示
- 登录用户可发表评论

## 四、运行方式

### 4.1 初始化数据库

```bash
mysql -u root -p < sql/init.sql
```

### 4.2 修改数据库配置

编辑 `DBUtil.java` 文件，修改数据库连接信息。

### 4.3 启动项目

```bash
mvn clean tomcat7:run
```

### 4.4 访问应用

打开浏览器访问：http://localhost:8080/forum

## 五、实验心得

### 5.1 技术收获

1. **MVC架构理解**：通过本次实验，深入理解了MVC设计模式的思想，学会了如何将业务逻辑、数据访问、视图展示进行分离。

2. **DAO模式应用**：掌握了DAO设计模式，通过封装数据访问逻辑，提高了代码的可维护性和复用性。

3. **完整项目开发**：从数据库设计到前后端实现，完整经历了Web应用开发的全过程。

4. **技术整合能力**：将前几次实验学习的Servlet、JSP、JDBC技术进行整合，开发出功能完整的应用。

### 5.2 遇到的问题与解决

1. **中文乱码问题**：通过配置字符编码过滤器，统一设置请求和响应的编码为UTF-8。

2. **URL路径问题**：使用`${pageContext.request.contextPath}`确保路径正确。

3. **分页逻辑**：正确计算偏移量`(page-1)*pageSize`，处理边界条件。

### 5.3 改进方向

1. 添加用户头像功能
2. 实现帖子编辑和删除
3. 添加帖子分类功能
4. 实现搜索功能
5. 引入前端框架提升用户体验

## 六、参考资料

1. Java Servlet 4.0 Specification
2. JavaServer Pages 2.3 Specification
3. JDBC 4.0 Specification
4. MySQL 8.0 Reference Manual
5. 《Head First Servlets and JSP》

---

**实验完成日期**：2025年

**课程名称**：Web系统和技术

**学校**：太原理工大学
